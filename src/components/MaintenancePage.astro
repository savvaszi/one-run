<div class="maintenance-page">
  <video class="maintenance-bg" autoplay muted loop playsinline>
    <source src="/herovideo1.mp4" type="video/mp4" />
  </video>
  <div class="maintenance-overlay"></div>

  <div class="maintenance-content">
    <a href="/" class="logo">
      <span class="logo-one">one</span>
      <span class="logo-run">run</span>
      <span class="logo-sub">travel packages</span>
    </a>

    <h1 class="maintenance-motto">Which race is<br />your <em>next</em> one?</h1>

    <div class="maintenance-divider"></div>

    <p class="maintenance-notice">Page under construction — we will be live soon</p>
  </div>
</div>

<style>
  .maintenance-page {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: var(--bg);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .maintenance-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.35;
    z-index: 0;
  }

  .maintenance-overlay {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 50% 40%, transparent 0%, rgba(0,0,0,0.7) 100%);
    z-index: 1;
    pointer-events: none;
  }

  .maintenance-content {
    text-align: center;
    padding: 48px 24px;
    max-width: 600px;
    width: 100%;
    position: relative;
    z-index: 2;
    animation: fadeUp 1s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .maintenance-content .logo {
    display: inline-flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: center;
    margin-bottom: 48px;
    cursor: default;
    line-height: 1;
  }

  .maintenance-content .logo-sub {
    display: block;
    width: 100%;
    margin-top: 4px;
    text-align: center;
  }

  .maintenance-motto {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(36px, 5vw, 52px);
    font-weight: 500;
    line-height: 1.1;
    letter-spacing: -1px;
    color: var(--fg);
    margin-bottom: 24px;
  }

  .maintenance-motto em {
    font-style: italic;
    color: var(--accent);
    font-weight: 400;
  }

  .maintenance-divider {
    width: 40px;
    height: 1px;
    background: var(--accent);
    margin: 0 auto 24px;
    opacity: 0.6;
  }

  .maintenance-notice {
    font-family: 'DM Sans', sans-serif;
    font-size: 12px;
    font-weight: 300;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    color: var(--mute);
  }
</style>
