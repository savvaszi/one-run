<div class="maintenance-page">
  <div class="maintenance-content">
    <a href="/" class="logo">
      <span class="logo-one">one</span>
      <span class="logo-run">run</span>
      <span class="logo-sub">travel packages</span>
    </a>

    <div class="maintenance-runner">
      <img src="/Runner1.jpg" alt="" class="runner-img" />
      <div class="runner-shadow"></div>
    </div>

    <h1 class="maintenance-motto">Which race is<br />your <em>next</em> one?</h1>

    <div class="maintenance-divider"></div>

    <p class="maintenance-notice">Page under construction — we will be live soon</p>
  </div>
</div>

<style>
  .maintenance-page {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: var(--bg);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .maintenance-page::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 50% 35%, rgba(201,168,106,0.06) 0%, transparent 70%);
    pointer-events: none;
  }

  .maintenance-content {
    text-align: center;
    padding: 48px 24px;
    max-width: 600px;
    width: 100%;
    position: relative;
    z-index: 1;
    animation: fadeUp 1s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .maintenance-content .logo {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 48px;
    position: static;
    cursor: default;
  }

  .maintenance-content .logo-sub {
    position: static;
    margin-top: 4px;
  }

  .maintenance-runner {
    position: relative;
    width: 180px;
    height: 180px;
    margin: 0 auto 40px;
  }

  .runner-img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    animation: runnerPulse 2.2s ease-in-out infinite;
    position: relative;
    z-index: 1;
  }

  .runner-shadow {
    position: absolute;
    bottom: 8px;
    left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 8px;
    background: radial-gradient(ellipse, rgba(201,168,106,0.4) 0%, transparent 70%);
    border-radius: 50%;
    animation: shadowPulse 2.2s ease-in-out infinite;
  }

  @keyframes runnerPulse {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-8px) scale(1.04); }
  }

  @keyframes shadowPulse {
    0%, 100% { width: 60%; opacity: 0.4; }
    50% { width: 45%; opacity: 0.25; }
  }

  .maintenance-motto {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(36px, 5vw, 52px);
    font-weight: 500;
    line-height: 1.1;
    letter-spacing: -1px;
    color: var(--fg);
    margin-bottom: 24px;
  }

  .maintenance-motto em {
    font-style: italic;
    color: var(--accent);
    font-weight: 400;
  }

  .maintenance-divider {
    width: 40px;
    height: 1px;
    background: var(--accent);
    margin: 0 auto 24px;
    opacity: 0.6;
  }

  .maintenance-notice {
    font-family: 'DM Sans', sans-serif;
    font-size: 12px;
    font-weight: 300;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    color: var(--mute);
  }
</style>
